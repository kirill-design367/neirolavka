/**
 * Заглушка оплаты.
 *
 * Ничего не принимает и делает это честно: счёт не выставляется,
 * человеку говорится, что оплата скоро, заказ при этом создаётся
 * и уходит администратору. Так лавка работает уже сейчас — просто
 * деньги ходят мимо бота, а подтверждает их человек.
 *
 * Когда появится настоящий поставщик, он встанет на это же место:
 * тип один, вызывающий код не меняется.
 */

import type { PostavshchikOplaty, Schet } from './index.js';

export const zaglushka: PostavshchikOplaty = {
  imya: 'zaglushka',
  rabotaet: false,
  async vystavit(): Promise<Schet> {
    return {
      vneshnyId: null,
      adres: null,
      soobshchenie:
        'Оплата в боте пока не подключена. Заказ я записал и передал администратору — ' +
        'он напишет вам, как заплатить.',
      gotovoSrazu: false,
    };
  },
  razobratUvedomlenie() {
    // Настоящих уведомлений пока не бывает. Молча ничего не разбираем:
    // отвечать «принято» на то, чего мы не проверяли, нельзя.
    return null;
  },
};
